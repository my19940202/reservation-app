# 心理咨询预约小程序 — 技术文档

## 1. 项目概述

面向心理咨询机构的轻量预约系统，核心能力：**次数包 + 咨询师档期 + 课后核销**。  
三类角色：普通用户（预约）、咨询师（核销）、管理员（统计 + 改记录 + 只读查用户预约）。

- 小程序端：微信原生 + Vant Weapp + 微信云开发
- 管理后台：小程序内 `pages/admin/` 模块（非独立 Web）
- 预算定位：6k MVP

---

## 2. 本地开发

### 2.1 环境要求

- 微信开发者工具（基础库 ≥ 2.2.3）
- Node.js 18+
- 已开通云开发，并配置跨环境资源共享（见 2.3）

### 2.2 启动步骤

```bash
cd reservation-app
npm install
```

1. 用微信开发者工具打开项目根目录
2. 菜单：**工具 → 构建 npm**（生成 `miniprogram_npm/`）
3. 在云开发控制台创建数据库集合（见第 5 节）
4. 右键上传云函数：`login`、`appointment`、`verify`、`admin`（各目录先 **安装依赖**）
5. 编译运行，真机调试

### 2.3 云环境配置

[`app.js`](app.js) 使用跨账号/跨环境云资源：

| 配置项 | 值 |
|--------|-----|
| resourceEnv | `cloud1-5g5eyjtze161c202` |
| resourceAppid | `wx47cf005024af6c8f` |
| 小程序 AppID | `wx47cf005024af6c8f`（[`project.config.json`](project.config.json)） |

更换环境时同步修改 `app.js` 中上述两项。

---

## 3. 项目结构

```
reservation-app/
├── app.js / app.json / app.wxss
├── package.json                 # @vant/weapp
├── project.config.json
├── constants/enums.js           # 角色、状态、类型枚举
├── constants/collections.js     # 云数据库集合名（reserve_ 前缀）
├── utils/
│   ├── index.js                 # Toast、日期工具
│   ├── date.js
│   ├── auth.js                  # 角色判断
│   └── request.js               # 云函数封装
├── pages/
│   ├── home/                    # Tab：首页（剩余次数）
│   ├── teachers/                # 咨询师列表
│   ├── booking/                 # 选时段预约
│   ├── appointments/            # 我的预约
│   ├── appointment-detail/      # 预约详情 / 取消
│   ├── verify/                  # 教师核销
│   ├── profile/                 # Tab：我的（角色入口）
│   └── admin/
│       ├── index/               # 统计看板
│       ├── teacher-appointments/# 咨询师预约（可编辑）
│       ├── user-appointments/   # 用户预约（只读）
│       └── appointment-edit/    # 编辑咨询记录
├── cloudfunctions/
│   ├── login/
│   ├── appointment/
│   ├── verify/
│   ├── admin/
│   └── common/db.js             # 公共逻辑源文件（部署时复制到各函数）
├── images/                      # TabBar 图标
├── 决策.md
└── 技术文档.md
```

---

## 4. 技术选型

| 层级 | 选型 |
|------|------|
| 小程序 | 微信原生 |
| UI | Vant Weapp `@vant/weapp` |
| 后端 | 微信云开发（云数据库 + 云函数） |
| 管理后台 | 小程序 `pages/admin/` |
| 鉴权 | openId + `users.role` |

### TabBar

| Tab | 页面 |
|-----|------|
| 预约 | `pages/home/index` |
| 我的 | `pages/profile/index` |

教师核销、管理后台入口在「我的」页按角色显示。

---

## 5. 云数据库

在云开发控制台创建以下集合（统一 `reserve_` 前缀，常量定义见 [`constants/collections.js`](constants/collections.js)）：

| 集合名 | 说明 |
|--------|------|
| `reserve_users` | 用户 |
| `reserve_user_packages` | 次数包 |
| `reserve_teachers` | 咨询师 |
| `reserve_time_slots` | 档期 |
| `reserve_appointments` | 预约记录 |

### 5.1 `reserve_users`

| 字段 | 类型 | 说明 |
|------|------|------|
| name | string | 昵称/姓名 |
| phone | string | 手机号 |
| role | string | `user` / `teacher` / `admin` |
| teacher_id | string | 咨询师关联 reserve_teachers._id |
| created_at | date | |

系统字段 `_openid` 自动写入。

### 5.2 `reserve_user_packages`

| 字段 | 类型 | 说明 |
|------|------|------|
| _openid | string | 用户 openId（便于小程序端查询） |
| package_type | string | `monthly` / `individual` |
| start_date | date | 生效日 |
| end_date | date | 失效日 |
| one_to_one_total / one_to_one_remaining | number | 1v1 次数 |
| group_total / group_remaining | number | 1vN 次数 |
| status | string | `active` / `expired` |

### 5.3 `reserve_teachers`

| 字段 | 类型 | 说明 |
|------|------|------|
| name | string | 姓名 |
| avatar | string | 头像 |
| intro | string | 简介 |
| types | array | `['oneToOne','group']` |
| status | string | `active` / `inactive` |

### 5.4 `reserve_time_slots`

| 字段 | 类型 | 说明 |
|------|------|------|
| teacher_id | string | |
| date | string | YYYY-MM-DD |
| start_time / end_time | string | HH:mm |
| type | string | `oneToOne` / `group` |
| capacity | number | 名额上限 |
| booked_count | number | 已预约数 |
| status | string | `open` / `full` / `closed` |

### 5.5 `reserve_appointments`

| 字段 | 类型 | 说明 |
|------|------|------|
| user_id | string | |
| _openid | string | 预约用户 |
| teacher_id | string | |
| slot_id | string | |
| type | string | `oneToOne` / `group` |
| status | string | `booked` / `completed` / `cancelled` |
| booked_by | string | `self` / `admin` |
| verified_at / verified_by | date / string | 核销信息 |
| admin_note | string | 管理员备注 |
| updated_by / updated_at | string / date | 修改审计 |
| created_at | date | |

**建议索引：** `reserve_time_slots(teacher_id, date)`、`reserve_appointments(_openid, status)`、`reserve_appointments(teacher_id)`

---

## 6. 业务规则

```
预约成功 → 不扣次，仅占用时段名额
教师/管理员核销 → 扣次 1 次（幂等）
取消（booked） → 释放时段名额
completed 记录 → 不可再改状态/时段，仅可改备注
```

两种咨询类型：

- **oneToOne**：1v1，capacity = 1
- **group**：1vN 工作坊，capacity 由管理员配置

---

## 7. 云函数

| 云函数 | action | 说明 |
|--------|--------|------|
| login | — | 返回 openId，首次自动创建 user |
| appointment | create | 创建预约 |
| appointment | cancel | 取消预约 |
| appointment | list | 列表（scope: self / teacher） |
| appointment | detail | 详情 |
| verify | confirm | 核销并扣次 |
| admin | getStats | 统计看板 |
| admin | listByTeacher | 按咨询师查预约 |
| admin | listByUser | 按用户关键词查预约 |
| admin | updateAppointment | 修改状态/备注 |

部署：每个云函数目录内执行 `npm install` 后，在开发者工具中「上传并部署」。

---

## 8. 管理后台（小程序 admin）

| 页面 | 功能 |
|------|------|
| admin/index | 用户数、咨询师数、预约数、各状态统计 |
| admin/teacher-appointments | 按咨询师查看预约，可进入编辑 |
| admin/user-appointments | 按姓名/手机号搜索，只读 |
| admin/appointment-edit | 改状态（待咨询/已完成/已取消）、写备注 |

**鉴权：** 前端 `requireRole(['admin'])` + 云函数 `assertAdmin`。

---

## 9. MVP 与二期

**MVP 包含：**

- 用户：看次数、预约、我的预约、取消
- 教师：核销
- 管理员：统计 + 咨询师预约可改 + 用户预约只读

**二期（不加价不做）：**

- 在线支付/续费
- 订阅消息
- 教师 CRUD、放时段
- 复杂报表 / Excel 导出
- 咨询师自助排班

---

## 10. 签约前需确认

1. **1vN 每场人数上限**是多少？是否允许超员？
2. **扣次时机**：建议「核销扣次」；若客户要求「预约即扣次」需单独说明风险。

---

## 11. 初始化测试数据示例

在云开发控制台手动插入：

**reserve_teachers：** `{ name: "菲欧拉", intro: "心理咨询师", types: ["oneToOne","group"], status: "active" }`

**reserve_user_packages：** 给测试用户 openId 绑定 `{ one_to_one_remaining: 3, group_remaining: 5, status: "active", end_date: 一个月后 }`

**reserve_users：** 将管理员账号 `{ role: "admin", name: "管理员" }`；咨询师 `{ role: "teacher", teacher_id: "..." }`

**reserve_time_slots：** `{ teacher_id, date: "2026-05-30", start_time: "10:00", end_time: "11:00", type: "oneToOne", capacity: 1, booked_count: 0, status: "open" }`
